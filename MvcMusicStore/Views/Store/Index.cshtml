@model  IEnumerable <Genre>
    @{ 
        ViewBag.Title = "Store";
    }
<h3>Browse Genre</h3>

<p>
    Select from @Model.Count() genre:</p>
<ul>
    @foreach (var genre in Model)
    {
        
        <li>@Html.ActionLink(genre.Name,"Browse" , new { genre = genre.Name })</li>
    }
</ul>


